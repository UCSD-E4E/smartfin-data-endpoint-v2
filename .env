# PostgreSQL Connection Details
DB_NAME=smartfin_db
DB_USER=postgres
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432

# Django Settings
SECRET_KEY=change-this-in-production
DEBUG=False

# Comma-separated list of allowed hosts (e.g. your server IP or domain)
# ALLOWED_HOSTS=your-server-ip,yourdomain.com

# (Optional) If you have a single connection string:
# DATABASE_URL=postgres://user:password@host:port/dbname